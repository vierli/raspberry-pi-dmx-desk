# UART0 on GPIO 14/15 after enabling dtoverlay=uart0-pi5
DMX_PORT=/dev/ttyAMA0
DMX_REFRESH_HZ=30
DMX_FIXTURES_FILE=config/fixtures.json
# Set to true to test the web interface without DMX hardware.
DMX_SIMULATION=false

